\section{Results}

\begin{frame}{Performance Analysis}
	
\begin{block}{}
	The proposed model outperforms the baseline across all evaluated scenarios
\end{block}	
 
\begin{figure}[htbp]
	\centering
	\input{figures/MSLL_peformance_chdnormal.tex}
\end{figure}

\begin{block}{}
	This improvement is due to the latent GP component in the proposed model, which dynamically explains the noise in the data.
\end{block}

\end{frame}

\begin{frame}{One-day-ahead forecasting}
	
%\begin{block}{}
%	For the baseline model, the width of the confidence interval remains constant over time. The proposed model chooses to explain that dynamic with a wider confidence interval, indicating a preference to account for peaks with higher uncertainty.
%\end{block}


\begin{figure}[htbp]
	\centering
	\begin{tikzpicture}[font=\scriptsize]
		\begin{groupplot}[
			group style={
				group size=2 by 1,
			},
			width=0.5\linewidth,
			height=0.32\linewidth,
			xmin=200, xmax=400,
			ymin=-212629.992846074, ymax=11354433.3118096,
			enlarge x limits=false,
			enlarge y limits=false,
			restrict x to domain=200:400,
			restrict y to domain=-212629.992846074:11354433.3118096,
			unbounded coords=discard,
			clip=true,
			xlabel={Time (days)},
			grid=both,
			major grid style={line width=.5pt, draw=gray!30},
			minor grid style={line width=.3pt, draw=gray!15},
			tick style={black, thick},
			axis x line=bottom,
			axis y line=left,
			xtick pos=bottom,
			ytick pos=left,
			tick align=outside,
			title style={font=\normalsize, color=BrandBrown},
			legend style={
				at={(rel axis cs:0.98,0.98)},
				anchor=north east,
				legend columns=1,
				/tikz/every even column/.append style={column sep=1em},
				inner sep=2pt,
				font=\scriptsize,
				nodes={scale=0.9, transform shape},
				draw=black!25,
				fill=white,
				fill opacity=0.85,
				legend cell align=left
			}
			]
			\nextgroupplot[
			ylabel={Streamflow (kWh)},
			ylabel style={at={(axis description cs:0.1,0.5)}, anchor=south}, % ylabel closer to canvas
			title={Baseline}
			]
			\input{figures/lmc_forecasting_AMANI}
			
			\nextgroupplot[
			title={Proposed},
			yticklabels={} % keep grid, remove labels
			]
			\input{figures/chd_normal_forecasting_AMANI}
			
		\end{groupplot}
	\end{tikzpicture}
\end{figure}


\begin{multicols}{2}
	The confidence interval remains practically constant over time, even when the data changes slowly.
	
	\columnbreak
	
	The model explains the peaks with higher uncertainty while maintaining a narrow confidence interval when the data changes slowly.
\end{multicols}

	\end{frame}